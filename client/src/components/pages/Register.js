import React from 'react';
import Form from '../Form';

const Register = () => {
  return (
    <main>
      <div className="login__image" />
      <Form register="true" />
    </main>
  );
};

export default Register;
